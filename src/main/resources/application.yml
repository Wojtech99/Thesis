server:
  port: 9092

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  liquibase:
    change-log: classpath:dbChangeLog/master.xml

  servlet:
    multipart:
      enabled: true

staticImageDirectory: "/src/main/resources/images/"
